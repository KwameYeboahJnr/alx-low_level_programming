#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string
 *@s: string pointer
 *Return: returns length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
